FROM mukerjee/sdrt-base

MAINTAINER Matt Mukerjee "mukerjee@cs.cmu.edu"

RUN apt-get update && apt-get install -y \
                              iperf \
    && rm -rf /var/lib/apt/lists/*

ENTRYPOINT ["iperf"]
CMD ["-s"]
